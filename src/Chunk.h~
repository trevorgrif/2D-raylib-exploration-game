#ifndef CHUNK_H
#define CHUNK_H

#include <iostream>
#include "Block.h"

class Chunk{
 private:
  Vector2 ChunkPos;
  std::vector<Block*> BlockList;
 public:
  Chunk(Vector2 Pos, float NoiseValues[256]);
  ~Chunk();
  
  void DrawChunk();
  Block* GetBlock(Vector2 Pos);
};

#endif //CHUNK_H
